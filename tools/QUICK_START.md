# IgnoreAreasClasses 快速开始指南

## 🚀 快速启动

```bash
cd /srv/code/firescrew/tools
./start-editor.sh        # 默认端口 8090
# 或指定端口
./start-editor.sh 8080
```

然后在浏览器中打开: `http://localhost:8090`

## 📐 坐标格式说明

**重要**: coordinates 字段格式为 `"top,bottom,left,right"` (4个整数)

```
坐标系统：
  0,0 ──────► X (left → right)
   │
   │
   ▼
   Y (top → bottom)

矩形区域示例：
   left=100        right=800
      │               │
  top=200 ────────────┤
      │   忽略区域    │
      │               │
bottom=600 ───────────┤
```

## 📝 配置格式

```json
{
  "ignoreAreasClasses": [
    {
      "class": ["car", "truck"],
      "coordinates": "200,600,100,800"
    }
  ]
}
```

**解析说明**:
- `top = 200` (Y坐标，上边界)
- `bottom = 600` (Y坐标，下边界)
- `left = 100` (X坐标，左边界)
- `right = 800` (X坐标，右边界)

## ✏️ 使用步骤

1. **加载图片**
   - 输入视频流地址: `http://localhost:8042`
   - 或上传本地图片

2. **选择类别**
   - 点击要忽略的检测类别（如 car, truck, person）
   - 可多选
   - 可添加自定义类别

3. **绘制区域**
   - 在图片上点击并拖拽绘制矩形
   - 矩形会自动转换为 `top,bottom,left,right` 格式

4. **保存区域**
   - 点击"完成当前区域"
   - 可继续绘制多个区域

5. **复制配置**
   - 点击"📋 复制到剪贴板"
   - 粘贴到 config.json 的 `ignoreAreasClasses` 字段

## 🎯 实际应用示例

### 场景1: 忽略画面左侧的行人检测

```json
{
  "class": ["person"],
  "coordinates": "0,1080,0,200"
}
```
- 区域: 左侧竖条（X: 0-200, Y: 0-1080 全高）

### 场景2: 忽略画面顶部的车辆检测

```json
{
  "class": ["car", "truck"],
  "coordinates": "0,150,0,1920"
}
```
- 区域: 顶部横条（X: 0-1920 全宽, Y: 0-150）

### 场景3: 忽略画面底部中间区域

```json
{
  "class": ["person", "car"],
  "coordinates": "900,1080,600,1320"
}
```
- 区域: 底部中间矩形（X: 600-1320, Y: 900-1080）

## ⚠️ 常见错误

### 错误信息
```
Error parsing config file: coordinates string must contain 4 comma separated integers
```

### 原因
- coordinates 字段不是4个整数
- 格式错误（如使用了多边形顶点坐标）

### 解决方法
确保 coordinates 格式为: `"top,bottom,left,right"`

❌ 错误示例:
```json
"coordinates": "100,200,300,200,300,400,100,400"  // 8个值，多边形格式
```

✅ 正确示例:
```json
"coordinates": "200,400,100,300"  // 4个值: top,bottom,left,right
```

## 🔍 验证配置

启动 firescrew 后检查日志:
```bash
docker logs -f firescrew-container-name
```

如果看到类似输出，说明配置正确:
```
[INFO] Ignore Areas Classes:
[INFO]   Class: [car truck], Coordinates: 200,400,100,300
```

## 📊 分辨率说明

常见视频分辨率:
- 1080p: 1920 x 1080
- 720p: 1280 x 720
- 4K: 3840 x 2160

确保你的坐标在视频分辨率范围内！
